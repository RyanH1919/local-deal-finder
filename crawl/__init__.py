"""Flow 2 grid catalogue crawl.

Geographic-grid collection layer. `crawl.grid` holds the cell math (geohash + GTA
config); `crawl.catalogue.crawl_cell` crawls a single cell. See
docs/flow2_grid_design.md. The scheduler wiring is Phase 2.
"""
